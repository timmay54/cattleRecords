/*       
*this is a support file
*
*/

#include <stdio.h>

extern float decim;

long variableLength(int varPassed) {
	printf("The length of variable is %d \n", sizeof(varPassed) );
}

long varIs(int varPassed) {
	printf("%s\n", );
}